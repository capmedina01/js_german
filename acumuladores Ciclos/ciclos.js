// ciclo while

const n = parseInt(prompt("Ingrese el número de notas"))
let acumulado = 0
let contador = 1

while(contador <= n){
    let nota = parseFloat(prompt("Ingrese "))
    contador ++
    console.log(contador)
}
